// ─────────────────────────────────────────────────────────────────────────
//  Supabase configuration
//
//  Fill these three values in after you create your Supabase project.
//  See README.md for exactly where to find them.
// ─────────────────────────────────────────────────────────────────────────

// Your project URL, e.g. "https://abcdefgh.supabase.co"
const SUPABASE_URL = "YOUR_SUPABASE_URL";

// Your project's "anon" / public API key (safe to expose in the browser —
// Row Level Security on the database is what actually protects your data).
const SUPABASE_ANON_KEY = "YOUR_SUPABASE_ANON_KEY";

// The email of the single admin account you create in Supabase Authentication.
// Admins only ever type the password; this email is used behind the scenes.
const ADMIN_EMAIL = "admin@skating.local";